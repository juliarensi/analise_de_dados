
# Usando operadores lógicos para indexação

View(mtcars)
mtcars$disp >= 400 & mtcars$wt <= 1 #testa se, numa dada linha, os valores na disp são maiores ou iguais a 400 OU se os valores em wt são iguais ou menores que 1
